import 'package:flutter/material.dart';

class MyRoomsPage extends StatelessWidget {
  const MyRoomsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("My Rooms Page"),
    );
  }
}